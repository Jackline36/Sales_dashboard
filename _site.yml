name: Bidhaa Sasa
sidebar:
      - text: How well a client is paying
        module:
          metric_panel:
           metric: !expr list.load("list.rds")$test_analysis_avg_trp
      - text: Total amount Paid
        module:
          metric_panel:
           metric: !expr list.load("list.rds")$test_analysis_nb_total_amount
      - text: Total loan uptake
        module:
          metric_panel:
           metric: !expr list.load("list.rds")$test_analysis_avg_loan_principal
      - text: Total Down payment
        module:
          metric_panel:
           metric: !expr list.load("list.rds")$test_analysis_avg_down_payment
      - text: Keeping tracks on Products
        module:
          metric_panel:
            metric: !expr list.load("list.rds")$test_analysis_nb_product
      - text: Cheking which items are complementary
        module:
          metric_panel:
            metric: !expr list.load("list.rds")$test_analysis_nb_product
      - text: Installments and progess - monitor that! Tounche!!
        module:
          metric_panel:
            metric: !expr list.load("list.rds")$test_analysis_nb_installments
